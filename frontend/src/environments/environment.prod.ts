/**
 * Configuración de ambiente de producción.
 * La URL de la API se configura según el despliegue.
 */
export const environment = {
  production: true,
  apiUrl: "https://your-backend-url.onrender.com/api/v1",
};
